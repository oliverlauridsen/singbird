import React from "react";
import styled from "styled-components";

export default function Profile() {
	return <MainContent>Profile</MainContent>;
}

const MainContent = styled.section`
	width: 100%;
	padding: 50px 75px;
`;
