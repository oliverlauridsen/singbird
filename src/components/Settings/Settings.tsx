import React from "react";
import styled from "styled-components";

export default function Settings() {
	return <MainContent>Settings</MainContent>;
}

const MainContent = styled.section`
	width: 100%;
	padding: 50px 75px;
`;
